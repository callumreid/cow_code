# Pin the local dev pipeline to the STAGING data plane (with no .env everything resolves to PROD).
MONOLITH_DATABASE_SECRET_NAME=staging/rds/monolith/coval_app
ORGANIZATION_ROUTING_TABLE_NAME=organization-db-routing-staging
API_KEY_ROUTING_TABLE_NAME=api-key-routing-staging
